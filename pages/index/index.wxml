<!--index.wxml-->
<scroll-view>
  <view class="item_goods" wx:for="{{array}}">
  <image class="image_goods_pic" src="{{item.pic}}"/>
    <text class="text_subtitle">{{index}}: {{item.subtitle}}</text>
</view>
</scroll-view>